### fork()
fork is one of the system call which is used create a child process which is the exact copy of the oarent process
execept the process id

### exec()
exec() is one of the system call which is used to replace the exisintg process within the new process
it loads the new program into the process memory and after succesful completion the curent program is complea
tely replaced by the new program
